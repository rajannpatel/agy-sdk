#!/usr/bin/env bash
set -euo pipefail

# Ensure the .gemini directory exists
mkdir -p /home/workshop/.gemini

# Run native setup/install as the workshop user
sudo -u workshop --login "${SDK}/bin/agy" install
