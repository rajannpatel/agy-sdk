summary: Launch a workshop with the agy SDK

prepare: |
  envsubst '${BASE}' <workshop.yaml.in >workshop.yaml
  workshop launch

restore: |
  workshop remove || true

execute: |
  # `workshop launch` is a blocking, fail-fast call. Verify that the workshop is interactable.
  workshop exec -- true

debug: |
  workshop tasks
